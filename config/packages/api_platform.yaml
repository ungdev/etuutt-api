api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]

    # Set this to false if you want Webby to disappear (the spider on the right of the swagger)
    show_webby: false
